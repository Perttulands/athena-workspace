# Uncertain — Needs Review

These findings need human review to determine if they're bugs or intentional.

## silent-fallback.hidden-failure-bash (163 findings)
`2>/dev/null` or `|| true` patterns. Some are justified, some hide real errors.

### By repo:
- argus: 85 findings
- learning-loop: 75 findings
- truthsayer: 3 findings

### Top offenders (need REASON comments or fix):
- `learning-loop/tests/test-backfill.sh:62` — output="$("$BACKFILL" 2>&1 || true)"...
- `learning-loop/tests/test-backfill.sh:66` — output="$("$BACKFILL" /nonexistent/path 2>&1 || true)"...
- `learning-loop/tests/test-backfill.sh:73` — assert "no feedback files for empty runs" '[[ $(command ls "...
- `learning-loop/tests/test-backfill.sh:92` — assert "creates 3 feedback files" '[[ $(command ls "$tmp/fee...
- `learning-loop/tests/test-backfill.sh:100` — assert "skips running, only 1 feedback file" '[[ $(command l...
- `learning-loop/tests/test-detect-patterns.sh:261` —   "$(echo "$result" | jq 'type == "array"' 2>/dev/null || ec...
- `learning-loop/tests/test-detect-patterns.sh:265` —   "$(echo "$result_clean" | jq 'type == "array"' 2>/dev/null...
- `learning-loop/tests/test-detect-patterns.sh:275` —   "$(jq empty "$REGISTRY_FILE" 2>/dev/null && echo true || e...
- `learning-loop/tests/test-detect-patterns.sh:287` — usage_output="$("$DETECTOR" 2>&1 || true)"...
- `learning-loop/tests/test-guardrails.sh:39` — usage_out="$("$GUARDRAILS" --help 2>&1 || true)"...
- `learning-loop/tests/test-manage-patterns.sh:57` — usage_output="$("$MANAGE" 2>&1 || true)"...
- `learning-loop/tests/test-manage-patterns.sh:122` — detail_unknown="$("$MANAGE" detail nonexistent-pattern 2>&1 ...
- `learning-loop/tests/test-manage-patterns.sh:220` — missing_output="$("$MANAGE" list 2>&1 || true)"...
- `learning-loop/tests/test-notify.sh:74` — output="$("$NOTIFY" 2>&1 || true)"...
- `learning-loop/tests/test-notify.sh:119` — output="$(WAKE_GATEWAY="$MOCK_GATEWAY" "$NOTIFY" unknown-eve...
- `learning-loop/tests/test-notify.sh:136` — output="$(WAKE_GATEWAY="$FAIL_GATEWAY" "$NOTIFY" weekly-repo...
- `learning-loop/tests/test-refine-prompts.sh:47` —   actual="$(jq -r "$query" "$file" 2>/dev/null || echo "JQ_E...
- `learning-loop/tests/test-refine-prompts.sh:165` — output="$(bash "$SCRIPT" --help 2>&1 || true)"...
- `learning-loop/tests/test-refine-prompts.sh:171` — output="$(bash "$SCRIPT" 2>&1 || true)"...
- `learning-loop/tests/test-retrospective.sh:98` — out="$(FEEDBACK_DIR=/nonexistent "$RETRO" --help 2>&1 || tru...

## bad-defaults.unvalidated-env-bash (102 findings)
Environment variables with silent defaults. May be intentional.

### By repo:
- learning-loop: 51 findings
- argus: 48 findings
- truthsayer: 2 findings
- relay: 1 findings

## error-context.unwrapped-error (166 findings)
Errors returned without context wrapping. Some are fine at boundaries.

### By repo:
- ludus-magnus: 64 findings
- relay: 40 findings
- truthsayer: 25 findings
- senate: 19 findings
- oathkeeper: 18 findings

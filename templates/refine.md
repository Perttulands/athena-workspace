# Refinement Pass

Merged branches: {BRANCHES}
Project context: {PROJECT_CONTEXT}

## Objective

Refine the codebase end-to-end after multi-agent contributions. Edit directly in the current branch.

## Checklist

1. **Integration** — imports resolve, APIs match, types align, no logical conflicts between agents
2. **Normalize** — consistent naming, error handling, logging, and style across all modules
3. **Cleanup** — remove dead code, unused imports, debug prints, stale TODOs, agent artifacts
4. **Polish** — clear error messages, consistent CLI help, docs match implementation
5. **Verify** — full test suite passes, build succeeds, linter clean, no hardcoded secrets

## Output

Provide: summary of substantive changes, verification commands and results, residual risks.
